# AutoFlowNG Landing Page — Environment Variables

# The URL of your backend (used if you add API calls to landing page)
NEXT_PUBLIC_API_URL=https://autoflowng-backend.up.railway.app

# The URL of your app dashboard (for "Get Started" / "Login" CTAs)
NEXT_PUBLIC_APP_URL=https://app.autoflowng.com

# Vercel Analytics is already wired in — no key needed, auto-detected on Vercel
